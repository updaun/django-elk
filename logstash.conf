input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  # 예시: 로그 메시지에서 특정 필드를 파싱하거나 변환할 수 있습니다.
  # 필요한 경우 추가적인 필터 설정을 넣으세요.
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logstash-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}
